#include "music_randomizer.h"

RECOMP_CALLBACK("APCpp-Glue", rando_solo_generate) bool music_rando_init()
{
    // First approach: Just randomize seq pointers and seqFont pointers.
    for (int i = 0; i < gSequenceTable; i++)
    {
        
    }
}